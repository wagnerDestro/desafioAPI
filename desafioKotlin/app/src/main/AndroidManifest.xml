<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.desafiokotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DesafioKotlin">
        <activity android:name=".ExtratoActivity"></activity>
        <activity android:name=".BloquearContaActivity" />
        <activity android:name=".SaqueActivity" />
        <activity android:name=".ConsultarSaldoActivity" />
        <activity android:name=".DepositarActivity" />
        <activity android:name=".CriarContaActivity" />
        <activity android:name=".CriarPessoaActivity" />
        <activity android:name=".InfoContaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>